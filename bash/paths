# Add Home bin for misc stuff
export PATH=$PATH:$HOME/bin

[[ -s ~/.nvm/nvm.sh ]]      && source ~/.nvm/nvm.sh      # This loads NVM
[[ -s ~/.rvm/scripts/rvm ]] && source ~/.rvm/scripts/rvm # Load RVM into a shell session *as a function*
